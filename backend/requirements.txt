# Pinned. A hackathon loses hours to "works on my laptop" - these exact versions
# are verified together, so `pip install -r requirements.txt` reproduces the
# demo environment on any teammate's machine (CLINCH.md 5, Failure Point 4).
fastapi==0.115.6
starlette==0.41.3
pydantic==2.10.4
uvicorn[standard]==0.34.0
httpx==0.28.1
pytest==8.3.4
bcrypt==4.2.1
PyJWT==2.10.1
